{
  "name": "docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve --incremental --quiet",
    "build": "eleventy",
    "build:verbose": "APIDOCS_VERBOSE=1 eleventy",
    "preview": "http-server _site -p 3000 -c-1",
    "clean": "rm -rf _site"
  },
  "dependencies": {
    "@carrotsearch/eleventy-apidocs": "workspace:*"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "http-server": "^14.1.1"
  }
}
