{
  "name": "apidocs-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm -F sample-docs dev",
    "build": "pnpm -F sample-docs build",
    "clean": "pnpm -F sample-docs clean",
    "test": "pnpm -F @carrotsearch/eleventy-apidocs test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.11.0"
  }
}
