{
  "name": "apidocs-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm -F docs dev",
    "build": "pnpm -F docs build",
    "preview": "pnpm -F docs preview",
    "clean": "pnpm -F docs clean",
    "test": "pnpm -F @carrotsearch/eleventy-apidocs test",
    "format": "biome format --write",
    "lint": "biome lint",
    "check": "biome check",
    "check:fix": "biome check --write"
  },
  "packageManager": "pnpm@11.2.2",
  "engines": {
    "node": ">=20.11.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2"
  }
}
