packages:
  - "apidocs"
  - "sample-docs"

# pnpm 11+ ignores postinstall scripts by default. These two ship native
# binaries (esbuild's compiler, sharp's libvips) and need their build step
# to run for the toolchain to work.
allowBuilds:
  esbuild: true
  sharp: true
